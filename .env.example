# BloodLink Network - Environment Variables Template
# Copy this file to .env before starting the development server:
# Windows (PowerShell): Copy-Item .env.example .env
# Linux / macOS (Bash):  cp .env.example .env

# Backend API Endpoint Base URL (Default proxy to local backend or staging server)
VITE_API_URL=/api/v1
